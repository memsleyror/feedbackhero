<h1>Give Feedback</h1>

<%= @requestedfeedback.user.first_name %> <%= @requestedfeedback.user.last_name %>
has made this request:
<%= @requestedfeedback.request_body %><br/>
for this situation:
<%= @requestedfeedback.situation %><br/>

<%= form_for @requestedfeedback do |f| %>
	<p>

		<%= f.label :positive %><br/>
		<%= f.text_area :positive %><br/>
		<%= f.label :growth_area %><br/>
		<%= f.text_area :growth_area %><br/>

		
		<%= f.hidden_field :hide, :value => true %>
		<%= f.hidden_field :responded_to, :value => true %>
		
	</p>

	<p>
		<%= f.submit "Update" %>
	</p>


<% end %>