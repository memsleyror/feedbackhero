
<h1>My View</h1>

<% @feedbacks.each do |feedback| %>
	<%= feedback.user_id %><%= feedback.situation %><%= feedback.positive %><%= feedback.growth_area %><%= feedback.hide %><%= feedback.from_user %><br />

<% end %>